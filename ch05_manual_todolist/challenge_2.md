现在这个 TODO List 已经有分类和优先级了，我想在上面继续迭代，加一个"截止日期"功能。
具体想法：
- 添加待办的时候可以选一个截止日期（可选，不填就没有 deadline）
- 列表里把截止日期显示出来；今天到期的标黄，已经过期的标红，已完成的不再提示颜色
- 排序规则在原来的基础上微调一下：未完成 > 已过期优先 > 优先级 > 创建时间倒序
- 支持后续修改截止日期（PATCH 接口加上 due_date 字段）

请基于现有代码改造：
1. 数据库要加一列 due_date（注意老数据要兼容，不能丢）
2. 后端 API 校验：due_date 要么不传，要么是合法的 YYYY-MM-DD 字符串
3. 前端在 TodoInput 加一个日期选择器，在 TodoItem 显示日期 + 状态颜色
4. 类型定义前后端都要同步

改完之后告诉我哪些文件动了，方便我对照看 diff。
